Linked lists are a favorite topic of mine. primarily because its simple.

its format is pretty simple:

there are nodes -> each node has the data, and the pointer towards the next node.

there is always a head node, and a end node pointing towards null.